# 2026-08-01 W1 v3.8-rc1 파일럿 원장

> 운영 정본: `docs/AI_업무분담_운영규정_v3.7.md`
> 파일럿 규칙: `docs/AI_업무분담_운영규정_v3.8-rc1.md`
> 정본 교체: MEDIUM과 HIGH 파일럿 평가 전까지 금지

## PILOT-000 — W1E clean checkpoint

- W1E closeout exact SHA:
  `64761f126cd395bb6057237a337fdd01ed511779`
- branch: `codex/w1e-assignment`
- local/upstream/remote: exact SHA 일치
- dirty: `0`; ahead/behind: `0/0`
- W1E는 v3.7로 시작해 v3.7로 닫았다. v3.8-rc1을 소급 적용하지 않는다.
- 이 clean checkpoint 뒤 새 슬라이스부터 rc1 파일럿을 시작한다.

## PILOT-M-001 — MEDIUM runner 파일럿 봉인

- 시작: `2026-08-01T21:57:25.371+09:00`
- base: `64761f126cd395bb6057237a337fdd01ed511779`
- packet: `review/packets/W1_RC1_MEDIUM_RUNNER_PACKET_v1.0.md`
- 위험: `MEDIUM`; 제품·DB 영향 없음
- 단일 제작자 write path: runner, focused PowerShell test, 이 원장
- 네트워크/API key/secret/DB/browser: 금지
- 다음 상태: `SEALED_FOR_RED`

### 비교 기준

- v3.7 유사 runner 보정:
  `217acc3a321df5b9a22afdd5f8a6d10d53ffdffa` →
  `285c04b908d456793293a81b7adf00848e905051`
- commit timestamp wall-clock proxy: `13m 13s`
- 연관 실패 실행: `39.5s`, `16/16` turns, tool calls `23`, total tokens
  `231399`, changed paths `0`, cost `uninstrumented`
- 한계: commit timestamp는 active time이 아니고 실제 비용 기준선도 없다.

### 시작 상태

- provider call: `0`
- retry: `0`
- no-patch call: `0`
- handoff: `0`
- LIVE_PROVIDER_API: `NOT_RUN`
- LIVE_POSTGRES: `NOT_RUN`

## PILOT-M-002 writer RED evidence

- Evidence time: `2026-08-01T22:04:22.8410097+09:00`; actual cwd: `C:\Users\USER\.codex\worktrees\d946\2.1`.
- Sealed base/head: `bc8bbd307b8565f15abafc207ec7f66f4150b433`; branch/ref: detached (`branch --show-current` empty, `symbolic-ref` empty); start status was clean and runner remained byte-unchanged during RED.
- Initial packet lookup at the delegated environment path returned not found; exact authority path used was `review/packets/W1_RC1_MEDIUM_RUNNER_PACKET_v1.0.md`, confirmed present at the sealed SHA. This was a non-mutating path mistake, not a test failure.
- First writer action was only creation/repair of `scripts/test-invoke-deepseek-workspace.ps1`; no runner edit, stage, commit, push, dependency, provider, database, API, browser, or runtime action occurred.
- Test SHA-256: `981A7596385CCCFF01A76B7B7242878B264E4C72F747CFC6D1B1B85F95BB2376`.
- Runner SHA-256 before any runner edit: `05F1FB337B022C51867699CD10548568D683C852E42B02584EB2F5818EFB78C7`.
- New-test diff evidence: `git diff --no-index --numstat -- /dev/null scripts/test-invoke-deepseek-workspace.ps1` exit `1` (expected for an untracked addition), `243 0`, exact changed path only the test. `git status --short` showed only `?? scripts/test-invoke-deepseek-workspace.ps1`.
- PS5.1 AST command: `powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command <Parser.ParseFile for test and runner>`; exit `0`; test errors `0`, runner errors `0`.
- Focused RED command: `powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File .\scripts\test-invoke-deepseek-workspace.ps1`; exit `1`; assertions `31` total, `16` passed, `15` failed.
- The pre-network child used an absent random temp `-EnvFile` path with `-JsonOutput`; it exited `1`, returned JSON `status=FAIL`, `error=DEEPSEEK_ENV_FILE_MISSING`, and no `https://api.deepseek.com`, `SendAsync`, or `HttpClient` marker. This proves the harness path/parse/env guard passed and the RED failures below are contract assertions.

### RED expected/actual contract failures

- Omitted timeout: expected nullable omitted input and provider effective `DeepSeek=300`/`OpenRouter=420`; actual `[int] RequestTimeoutSeconds = 180` and no effective provider default.
- Effective HTTP timeout: expected the resolved timeout in `HttpClient.Timeout`; actual raw `$RequestTimeoutSeconds` is used.
- Output tokens: expected `ValidateRange(128,32768)` with default `16384`; actual `ValidateRange(128,65536)`; a `32769` probe reached missing-env execution instead of parameter rejection.
- DeepSeek model: expected no Flash fallback; actual `deepseek-v4-flash` is present.
- Cost semantics: expected request-count initialization/increment, observed-cost tracking, and `uninstrumented` when a request lacks provider cost; actual those symbols/state are absent and cost remains decimal `0`.
- Shared reporting: expected JSON and text to expose the same effective timeout/read-budget/cost values; actual JSON uses the raw timeout and text omits timeout/read-budget fields.
- Pre-network result metadata: expected `request_count=0`; actual missing-env JSON has no `request_count` field.
- All safety/status-preservation assertions passed: sensitive-path denial, writer allowlist denial, provider URL fail-close, patch fail-close, and `PASS|PARTIAL|FAIL` literals.
- Test repair before sealing RED: the initial status assertion was order-sensitive; it was narrowed to presence of all three status literals. This was test-only and did not change the runner. Final RED counts above are after that repair.
- Provider call `0`; retries `0`; no-patch `0`; handoff `0`; tokens/cost for provider execution `uninstrumented` because no provider request was made; LIVE_PROVIDER_API=`NOT_RUN`; LIVE_POSTGRES=`NOT_RUN`.

## PILOT-M-002 final writer evidence

- Runner repair was one bounded writer round after valid RED. The focused test was then expanded test-only with explicit timeout `30/600`, rejection `29/601`, and OpenRouter omitted-timeout `420` pre-network probes; no runner contract was broadened by the expansion.
- Focused GREEN command: `powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File .\scripts\test-invoke-deepseek-workspace.ps1`; exit `0`; assertions `36` total, `36` passed, `0` failed.
- GREEN evidence covered omitted/effective DeepSeek `300`, OpenRouter `420`, explicit timeout preservation `30/600`, outside rejection `29/601`, read budget `12`, turns `16/24`, tokens `16384/32768`, DeepSeek pro/no Flash, request-count `0` pre-network cost `0`, JSON/text shared reporting source contracts, and unchanged safety/fail-close literals.
- Final PS5.1 AST command for both scripts: exit `0`; `scripts/test-invoke-deepseek-workspace.ps1` errors `0`; `scripts/invoke-deepseek-workspace.ps1` errors `0`.
- Final `git diff --check`: exit `0`; custom trailing-whitespace scan across all three allowed files: `0`.
- Final exact allowed-path check: status paths `3`, unexpected paths `0`, staged paths `0`; paths are exactly `scripts/invoke-deepseek-workspace.ps1`, `scripts/test-invoke-deepseek-workspace.ps1`, and `review/environment/office/2026-08-01_W1_RC1_PILOTS.md`.
- Final hashes: runner `AF71D6358F3055C603EA9D8A76CC22016BF0F0312CE76F91B3A89DC5CE584C8C`; final test `7DC5D38F5418453EB163686196BD8FC6BB87ABB239909CBF4D7E4D79360D7DA8`; ledger before this append `06D513B6302256F4D07139E2948AC5995AD66632CB6F1C25B1297BAD91BC1454`.
- Final test diff evidence: `git diff --no-index --numstat -- /dev/null scripts/test-invoke-deepseek-workspace.ps1` exit `1` as expected for untracked addition; `294 0`; no other untracked path.
- Cleanup: final test-specific temp artifact count `0`; active PowerShell process matching either runner/test script after completion `0`; all seven final GREEN child probes waited to exit and were disposed. No provider request, network call, secret-bearing file read, API-key read, PostgreSQL, Alembic, FastAPI, frontend, browser, or live runtime invocation occurred.
- Operational metrics: provider turns `0`; provider/API calls `0`; retries `0`; no-patch `0`; handoff `0`; provider tokens `uninstrumented` because no request was made; pre-network fail-close cost `0`; post-request no-usage `uninstrumented` branch is implemented but intentionally unexercised offline.
- Intermediate trouble/repair: one wrong initial packet-directory lookup (corrected to the packet path); one test patch-context retry; one oversized initial failure rendering; one order-sensitive status assertion repaired test-only; one post-RED test-only coverage expansion. No runner repair retry, provider retry, or network attempt.
- Timing: first recorded evidence `2026-08-01T22:00:37.7698518+09:00`; final evidence `2026-08-01T22:07:53.2265670+09:00`; observable wall-clock from first record `00:07:15.4567152`; active time not separately instrumented.
- Final state: `WRITER_READY`; HEAD remains `bc8bbd307b8565f15abafc207ec7f66f4150b433`, detached, unstaged and uncommitted. This is focused pilot GREEN evidence only; no independent PASS, pilot PASS, W1F PASS, or canonical promotion is claimed.

## PILOT-M-003 — Regina main integration

- Integration time: `2026-08-01T22:12:37.600+09:00`; main cwd:
  `C:\sswcenter\2.1`; branch `codex/w1e-assignment`; HEAD before candidate commit
  `bc8bbd307b8565f15abafc207ec7f66f4150b433`.
- Writer worktree에서 세 허용 파일만 회수했다. runner와 ledger는 첫 대조에서 writer
  SHA-256과 일치했다. test는 shell output의 종결 newline 한 개가 추가되어 첫 hash
  대조에서 불일치했고, 추가 newline만 제거한 뒤 writer hash
  `7DC5D38F5418453EB163686196BD8FC6BB87ABB239909CBF4D7E4D79360D7DA8`과
  일치시켰다.
- runner patch를 메인에 옮기는 첫 전사에서 text summary format 인수의
  `$script:Mode` 한 개를 누락했다. 실행 전 source 대조에서 발견해 writer exact line으로
  복구했고, 최종 runner hash
  `AF71D6358F3055C603EA9D8A76CC22016BF0F0312CE76F91B3A89DC5CE584C8C`가
  writer와 일치한 뒤에만 통합 gate를 실행했다. 이 중간 전사본은 candidate나 test
  증거로 사용하지 않는다.
- Main focused command:
  `powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File .\scripts\test-invoke-deepseek-workspace.ps1`.
  Exit `0`; assertions `36/36`, failed `0`.
- Main PS5.1 AST: runner errors `0`, test errors `0`; `git diff --check` exit `0`.
- Related process observer는 현재 관측 shell 자체를 포함해 before/after `1/1`로
  동일했고 새 잔존 process `0`; `w1-rc1-*` temp artifact `0`.
- Main integration 중 provider/API/network, secret/env read, PostgreSQL, Alembic,
  FastAPI, frontend, browser 호출은 모두 `0`이다.
- 현재 변경은 허용 세 경로뿐이며 candidate commit과 exact-SHA 독립검수 전이다.
- 첫 candidate staging 명령은 trailing-whitespace 오류문자열의
  `TRAILING_WHITESPACE:$path:$trailing`에서 PowerShell이 `$path:`를 잘못된 변수
  참조로 해석해 parser exit `1`로 body 진입 전에 종료됐다. stage·commit·파일 변경은
  `0`; format operator를 쓰는 동일 범위 명령으로 교정했다.

## PILOT-M-004 — independent review repair round 1

- Writer worktree의 local heading `PILOT-M-002`는 main의 기존 `PILOT-M-003` 뒤에
  병합하면서 순서만 `PILOT-M-004`로 바꿨다. 아래 repair evidence 내용은 유지했다.
- Review candidate: `90104e2604aeaa685ac553231ee2881890bf6049`; verdict: `REQUIRED_CHANGES`; candidate is stale and is not being treated as the current writer SHA.
- Exact P1: `scripts/test-invoke-deepseek-workspace.ps1` overstated dynamic coverage. It must dynamically, offline/pre-network, assert DeepSeek omitted timeout `300`; effective read budget/turn/token defaults and maxima; DeepSeek Writer and ReadOnly fallback `deepseek-v4-pro`; and exact text summary placeholder/argument mapping. Existing AST/source assertions remain supplemental only.
- Exact P2: every child probe must have a bounded timeout and safe kill/drain/dispose path so a regression cannot hang the test.
- Repair scope remains exactly the runner, focused test, and this ledger. Existing valid RED evidence above is preserved verbatim; no reset, discard, stage, commit, push, ref movement, dependency, secret, provider, API, database, browser, or runtime action is authorized.
- Repair start: `2026-08-01T22:28:32.9546196+09:00`; starting runner/test/ledger hashes were `AF71D6358F3055C603EA9D8A76CC22016BF0F0312CE76F91B3A89DC5CE584C8C`, `7DC5D38F5418453EB163686196BD8FC6BB87ABB239909CBF4D7E4D79360D7DA8`, and `9AC45B50CAE8B33C059E92D2B1A96F06C046F374AD10F906A7E6B9F93A864AE9`.

### Repair round 1 closeout

- Added an explicit `-OfflineConfig` runner path before env/key/provider resolution. It reports effective configuration without reading an env file or creating a network request; normal env model overrides and all existing safety/fail-close paths remain outside this opt-in path.
- Refactored the existing text summary through one shared formatter used by normal output and offline output. The strengthened test dynamically matched the exact summary for mode, dry_run, model, timeout, budget, max_tokens, turns, requests, and cost.
- Strengthened dynamic offline assertions: DeepSeek omitted timeout `300`; default read budget `12`, turns `16`, tokens `16384`; maxima turns `24`, tokens `32768`; ReadOnly and Writer fallback model `deepseek-v4-pro`; and zero-request decimal cost `0`.
- Every child probe now requires a timeout, uses asynchronous stdout/stderr drain, kills on timeout, waits for termination, and disposes the process. Final run used `12` bounded child probes: eleven runner/config probes at `15000ms` and one deliberate hanging PowerShell child at `250ms`; all lifecycle assertions passed.
- Strengthened focused command: `powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File .\scripts\test-invoke-deepseek-workspace.ps1`; exit `0`; assertions `65` total, `65` passed, `0` failed.
- PS5.1 AST before focused test: both scripts errors `0`, exit `0`. Final static check: AST errors `0`, `git diff --check` exit `0`, trailing whitespace `0`, unexpected paths `0`, staged paths `0`, temp artifacts `0`, active runner processes `0`.
- Final repair hashes before this ledger append: runner `B8FA82135AB934D7A3960DEE177C44A94A118A69463D00BD0E2212377B3CA336`; test `CAF1F31A2D4ECA240598CB1927700D4AE7FE295254658AB2D7ABD9532CC202E1`; ledger `7ECD6E26D64AC6969AB5D6D89E65063AC55FE25C84A1FDC8CA032EBF9029BE8E`.
- Repair timing: start `2026-08-01T22:28:32.9546196+09:00`; end `2026-08-01T22:33:40.4115134+09:00`; observable wall-clock `00:05:07.4568938`; active time not separately instrumented.
- Repair trouble: one malformed CIM filter quoting command returned a non-zero diagnostic; it did not modify files or start a lingering process. The corrected read-only cleanup check returned active runner processes `0`. Repair retries `0`; provider/API/network calls `0`; secret/credential reads `0`; PostgreSQL/Alembic/FastAPI/frontend/browser calls `0`; no-patch `0`; handoff `0`.
- Existing RED evidence and prior writer closeout remain unchanged above. Final state is writer-ready only; no independent PASS, pilot PASS, W1F PASS, or canonical promotion is claimed.

## PILOT-M-005 — repair candidate main integration

- Integration time: `2026-08-01T22:37:29.172+09:00`; main HEAD before repair commit:
  stale candidate `90104e2604aeaa685ac553231ee2881890bf6049`.
- 첫 자동 diff 이관은 standard unified numeric hunk header를 custom `apply_patch`가
  context anchor로 해석해 verification 전에 실패했다. 파일 변경은 `0`; hunk header를
  custom `@@` 형식으로 변환한 뒤 이관했다.
- Main runner/test SHA-256은 writer repair와 각각 정확히 일치한다:
  `B8FA82135AB934D7A3960DEE177C44A94A118A69463D00BD0E2212377B3CA336`,
  `CAF1F31A2D4ECA240598CB1927700D4AE7FE295254658AB2D7ABD9532CC202E1`.
- Main focused command:
  `powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File .\scripts\test-invoke-deepseek-workspace.ps1`.
  Exit `0`; assertions `65/65`; failed `0`.
- Dynamic evidence includes DeepSeek `300`, OpenRouter `420`, defaults/maxima,
  ReadOnly/Writer `deepseek-v4-pro`, exact text summary, and deliberate hanging-child
  timeout→kill→drain→dispose.
- Main PS5.1 AST runner/test errors `0/0`; `git diff --check` exit `0`.
- Related process observer before/after `1/1`로 새 잔존 process `0`; temp artifact `0`.
- Provider/API/network, secret/env read, PostgreSQL, Alembic, FastAPI, frontend,
  browser invocation은 모두 `0`이다.
- 현재 변경 경로는 runner, focused test, 이 ledger 세 개뿐이며 새 exact-SHA commit과
  동일 reviewer 방의 표적 재검수 전이다.

## PILOT-M-006 — independent repair re-review PASS

- Review end: `2026-08-01T22:45:29+09:00`; exact candidate
  `118a37325e8e62023922eda460750ca803145d0c`; direct parent
  `90104e2604aeaa685ac553231ee2881890bf6049`; sealed base
  `bc8bbd307b8565f15abafc207ec7f66f4150b433`.
- 동일 독립검수방이 clean 상태를 먼저 확인한 뒤 허가된 ref 이동을 한 번만 수행해 exact
  candidate에 detach했고 이후 ref를 움직이지 않았다. 실제 reviewer worktree는
  `C:\Users\USER\.codex\worktrees\c82e\2.1`이다.
- Verdict: `PASS`; findings `0`. Parent→candidate와 sealed-base→candidate 모두 변경은
  runner, focused test, 이 ledger의 정확히 세 경로이고 보호 대상 변경은 `0`이다.
- 독립 PS5.1 AST는 runner/test `0/0`; focused command exit `0`, assertions
  `65/65`, failed `0`; `git diff --check` 두 비교 모두 exit `0`; UTF-8 valid 세 파일,
  trailing whitespace `0`이다.
- P1은 DeepSeek timeout `300`, read budget `12`, turns `16/24`, tokens
  `16384/32768`, ReadOnly/Writer `deepseek-v4-pro`, exact text-summary mapping의 동적
  offline 검증으로 닫혔다. P2는 bounded child probe `12`개와 의도적 `250ms` hang의
  timeout→kill→async drain→wait→dispose 및 잔존 process `0`으로 닫혔다.
- Provider/API/network calls `0`; `LIVE_PROVIDER_API=NOT_RUN`;
  `LIVE_POSTGRES=NOT_RUN`; temp artifacts `0`. 실제 요청 뒤 `uninstrumented` cost 분기는
  source-reviewed only이며 residual evidence로 남는다.
- 이 PASS는 MEDIUM pilot closeout/push와 HIGH W1F 시작을 승인한다. v3.8 정본 승격은
  HIGH와 최종 평가 전까지 승인하지 않는다.

## PILOT-M-007 — post-PASS Claude canonical path note

- Check time: `2026-08-01T22:46:30.5439404+09:00`; main cwd
  `C:\sswcenter\2.1`; starting HEAD
  `118a37325e8e62023922eda460750ca803145d0c`; branch
  `codex/w1e-assignment`; starting dirty paths `0`.
- 사용자 요청에 따라 현행 정본 `docs/AI_업무분담_운영규정_v3.7.md`의 Direct provider
  절에 Claude Code 구독 CLI 절대경로 `C:\Users\USER\.local\bin\claude.exe`와 PATH
  조회 실패 시 절대경로 확인, 호출 전 `--version` exit `0`, OpenRouter API와의 분리
  규칙을 추가했다. 시범문서 `v3.8-rc1`은 이 단계에서 수정하지 않는다.
- Live local check: file exists `True`; Claude Code `2.1.217`; `--version` exit `0`;
  bytes `259460768`; last write `2026-07-22T19:58:12.9009650+09:00`;
  `Get-Command claude` result count `0`.
- 첫 read-only 검색에서 `rg`에 Windows shell wildcard 인수 `README*`를 직접 전달해
  OS error `123`으로 종료됐다. 파일 수정·stage·commit은 `0`; 이후 literal 경로와
  좁은 문서 범위로 재조회했다.
- 이 변경은 MEDIUM exact-SHA PASS를 소급 변경하지 않는 별도 docs/evidence 단계다.
