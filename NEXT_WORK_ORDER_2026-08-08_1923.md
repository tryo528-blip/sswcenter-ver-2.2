# 8월8일 19시23분자 다음 작업지시서

## 1. 작업 목적

수급자 기본 수정 및 신규 등록을 `수급자 + 보호자 2명 + 납부자 + 본인부담금 기간` 단위의 원자적 저장으로 완성하고, 현재 차단된 백엔드 연결을 복구한 뒤 실제 브라우저와 데이터로 검증한다.

현재 구현은 파일에 반영됐지만 테스트 및 브라우저 검증 전 상태다. 통과한 것으로 간주하지 말고 아래 순서대로 확인한다.

## 2. 절대 작업 범위

- 시작 위치: `E:\sswcenter\w1f-candidate`
- 실제 저장소 루트: `E:\sswcenter\w1f-candidate`
- frontend: `E:\sswcenter\w1f-candidate\frontend`
- backend: `E:\sswcenter\w1f-candidate\backend`
- 미리보기 URL: `http://127.0.0.1:5174`

작업 시작 즉시 다음 명령으로 위치를 확인한다.

```powershell
Set-Location -LiteralPath 'E:\sswcenter\w1f-candidate'
git rev-parse --show-toplevel
```

출력이 정확히 `E:\sswcenter\w1f-candidate`가 아니면 즉시 중단한다.

## 3. 금지 사항

- `E:\sswcenter\2.2` 사용 금지
- C 드라이브의 checkout, source, venv 사용 금지
- dependency 설치 또는 갱신 금지
- 제품 데이터의 프런트 하드코딩 금지
- unrelated 파일 수정 금지
- stage, commit, push 금지
- 알 수 없는 `5174` 포트 프로세스 종료 금지
- 테스트 실패를 숨기거나 미실행 검증을 통과로 보고하는 행위 금지

## 4. 현재 상태

### 4.1 미리보기

- Vite는 `E:\sswcenter\w1f-candidate\frontend`에서 `127.0.0.1:5174`로 다시 시작했다.
- 시작 당시 포트 소유 프로세스는 PID `16428`이었다.
- 다음 작업 시작 시 PID와 command line을 다시 확인한다.
- 동일 frontend의 Vite가 살아 있으면 재사용하고 종료하지 않는다.
- `VITE_DEV_LOGIN_BYPASS=true`는 실행 프로세스 범위에서만 사용한다.

### 4.2 차단 사항

- `GET http://127.0.0.1:5174/api/v1/recipients?...`가 `502 Bad Gateway`를 반환했다.
- 프런트 Vite는 살아 있지만 프록시 대상 백엔드가 연결되지 않는 상태다.
- 이 때문에 더미 수급자 주소 두 건은 아직 저장하지 못했다.

### 4.3 이번에 반영된 미검증 파일

- `backend/app/domains/recipient/detail_batch.py`
- `backend/app/api/recipient.py`
- `backend/app/domains/recipient/service.py`
- `backend/app/domains/w1c/service.py`
- `backend/app/domains/w1d/service.py`
- `frontend/src/services/recipientDetailBatchApi.ts`
- `frontend/src/pages/RecipientsPage.tsx`

## 5. 최우선 작업: 백엔드 연결 복구

1. 후보 frontend가 사용하는 API 프록시 대상과 환경값을 후보 저장소 안에서만 확인한다.
2. 기존 백엔드 프로세스와 포트를 확인한다.
3. 후보 저장소 안에 이미 승인된 실행 환경이 있을 때만 백엔드를 시작한다.
4. 후보 저장소에 실행 환경이 없거나 dependency가 누락됐으면 설치하거나 C 드라이브 환경을 빌려 쓰지 말고 `BLOCKED`로 보고한다.
5. 백엔드가 시작되면 `GET /api/v1/recipients`의 HTTP 상태와 응답 envelope만 확인한다.
6. Vite 프로세스는 종료하지 않고 유지한다.

## 6. 원자적 저장 구현 점검

### 6.1 API 계약

- 신규 등록: `POST /api/v1/recipients/basic-batch`
- 기본 수정: `POST /api/v1/recipients/{recipient_id}/basic-batch`
- 한 요청에 다음 내용을 포함한다.
  - 수급자 기본정보
  - 보호자 1, 2
  - 납부자 선택
  - 본인부담금 적용 기간
- 응답에는 저장된 수급자, 보호자, 저장 section 목록이 포함돼야 한다.

### 6.2 트랜잭션 조건

- 하위 서비스는 batch marker가 있을 때 `commit` 대신 `flush`만 한다.
- 최종 batch service가 한 번만 `commit`한다.
- 수급자, 보호자, 납부자, 본인부담금 중 하나라도 실패하면 전체를 `rollback`한다.
- 보호자만 먼저 저장되거나 본인부담금만 나중에 저장되는 부분 성공을 허용하지 않는다.
- row version 충돌 시 입력값을 보존하고 충돌 메시지를 보여준다.

### 6.3 본인부담금 기간

- 선택값: `일반`, `기초`, `6%`, `9%`
- 신규 등록 기본값: `일반`
- 변경일이 기존 기간 시작일보다 늦으면 기존 기간 종료일을 변경일 전날로 닫고 새 기간을 만든다.
- 기존 `MEDICAL_6`, `MEDICAL_9` 값은 같은 의미로 다시 저장할 때 임의로 다른 코드로 덮어쓰지 않는다.
- 화면에는 현재값만 표시하며 이력 UI는 이번 범위에 추가하지 않는다.

## 7. 프런트 화면 확인

### 7.1 헤더 버튼

- 수급자를 선택하면 `상세`, `수정`, `수급자 등록` 버튼이 보여야 한다.
- 수급자를 선택하지 않은 상태에서는 `수정` 버튼을 누를 대상이 없으므로 비활성 또는 미표시가 가능하다.
- 상세 화면에서는 `수급자 등록` 버튼이 필요하지 않다는 기존 결정을 유지한다.
- 상세 화면의 `수정`은 기본 수정 화면으로 튕기지 않고 상세 수정 상태를 유지해야 한다. 아직 충족되지 않았다면 별도 수정한다.

### 7.2 기본, 수정, 등록 폼

- 이름, 생년월일, 만 나이, 성별, 등급, 인정번호, 휴대전화, 본인부담금, 주소의 label과 value 위치가 세 화면에서 같아야 한다.
- input, select의 기본 브라우저 padding 때문에 value가 1~2px 움직이지 않도록 공통 control 스타일을 사용한다.
- 수정 화면은 별도 복제 폼을 아래에 추가하지 않고 기본 카드 자체를 편집한다.
- 주소는 우편번호, 앞주소, 상세주소 세 칸을 제공한다.

### 7.3 보호자

- 신규 등록과 기본 수정에서 보호자 1, 2를 같은 카드에서 편집한다.
- 보호자 필드: 이름, 관계, 전화번호, 이메일, 우편번호, 앞주소, 상세주소
- 수정 및 등록 상태에서 월별 테마색을 적용한다.
- 카드 바깥 배경이 아니라 각 항목 카드 안쪽에만 테마색을 적용한다.
- 납부자 체크는 두 보호자 중 하나만 선택 가능하며, 선택된 보호자 이름이 비어 있으면 저장을 막는다.

### 7.4 휴대전화 경고

- `수급자 등록` 버튼을 눌러 편집 화면을 여는 것만으로 경고를 띄우지 않는다.
- 실제 `저장`을 눌렀을 때 휴대전화가 비어 있는 경우에만 작은 경고 팝업을 표시한다.
- 화면 아래에 큰 인라인 오류 문구를 상시 표시하지 않는다.

## 8. 더미 수급자 두 건 주소 갱신

백엔드가 정상화된 뒤 기존 테스트 수급자 두 명만 수정한다. 새 수급자를 추가하지 않는다.

1. 테스트 김하늘
   - 우편번호: `04158`
   - 앞주소: `서울특별시 마포구 마포대로 109`
   - 상세주소: `101동 802호`
2. 테스트 박선우
   - 우편번호: `48058`
   - 앞주소: `부산광역시 해운대구 센텀중앙로 97`
   - 상세주소: `1203호`

반드시 각 수급자의 최신 `row_version`을 읽고 정상 PATCH 또는 완성된 batch API로 저장한다.

## 9. 별도 기록된 후속 기능

이번 원자적 저장과 섞지 말고 상태만 보고한다.

- 대시보드 업무 하위 항목 클릭 토글
- 클릭 시 완료 항목 흐리게 표시
- 대시보드 상단 API 오류 원인 확인
- 수급자 목록은 세는나이, 오른쪽 상세는 만나이 표시 유지
- 직원, 사회복지사, 본인부담금 화면 상단 띠의 배경색만 제거하고 구분선 유지

대시보드 업무 토글은 실제 완료 상태 저장 API와 계약이 확인된 뒤 구현한다. 단순 프런트 로컬 토글로 완료 처리하지 않는다.

## 10. 검증 순서

1. 변경 파일 정적 오류 확인
2. backend의 focused schema/service/API 검사
3. frontend의 focused type/build 검사
4. 신규 등록 정상 저장 확인
5. 기본 수정 정상 저장 확인
6. 보호자 또는 본인부담금에 의도적 실패를 발생시켜 전체 rollback 확인
7. 모바일 번호 공란 저장 경고 확인
8. 기본, 수정, 등록 폼의 label/value 좌표 비교
9. 더미 주소 두 건 표시 확인
10. Vite 포트와 브라우저 탭을 유지한 상태로 종료 보고

검증 명령은 기존 dependency로 실행 가능한 범위만 사용한다. dependency 설치가 필요하면 실행하지 말고 보고한다.

## 11. 완료 조건

- 백엔드 API가 `502`가 아닌 정상 응답을 반환한다.
- 신규 등록과 기본 수정이 각각 네 영역을 한 요청으로 저장한다.
- 강제 실패 시 부분 저장이 없다.
- 선택된 수급자에서 `수정` 버튼이 보이고 올바른 수정 모드로 진입한다.
- 기본, 수정, 등록 화면의 label과 value 위치가 일치한다.
- 보호자 카드도 등록과 수정에서 동일한 구조와 테마색을 사용한다.
- 본인부담금 `일반/기초/6%/9%`가 저장 후 다시 표시된다.
- 기존 테스트 수급자 두 명의 주소가 지정값으로 표시된다.
- Vite 서버와 인앱 브라우저 탭이 살아 있다.

## 12. 종료 보고 형식

- 시작 경로
- 실제 repo root
- frontend 및 backend 경로
- 사용한 실행 환경과 포트
- Vite 및 backend 포트 소유 PID와 command line
- 변경 파일 목록
- API별 HTTP 상태
- 실행한 검사 명령과 결과
- 신규 등록, 수정, rollback, 모바일 경고 결과
- 더미 주소 두 건 결과
- 브라우저 화면 표시 여부
- 미완료 또는 차단 사항
- Git 변경, stage, commit, push를 하지 않았다는 확인
