# 8월8월9시21분자

## 다음지시 작업서

### 1. 작업 대상

- 실제 저장소: `E:\sswcenter\w1f-candidate`
- 프런트엔드: `E:\sswcenter\w1f-candidate\frontend`
- 백엔드: `E:\sswcenter\w1f-candidate\backend`
- 다른 체크아웃, `E:\sswcenter\2.2`, C 드라이브 소스 및 가상환경을 사용하지 않는다.
- 별도 지시가 없으면 설치, stage, commit, push를 하지 않는다.

### 2. 현재 구현 상태

- 수급자 기본·수정·등록 화면은 같은 `input`과 `select`를 렌더링하도록 변경했다.
- 기본 화면은 `inert` 상태로 잠가 입력 요소의 글꼴과 좌표가 수정 화면과 달라지지 않게 했다.
- 보호자 화면은 동일 입력 요소를 유지하고 비활성 상태의 색상 및 투명도 차이를 제거했다.
- 본인부담금 카드 안에 선택값과 적용 시작일을 함께 배치했다.
- 본인부담금 화면 선택지는 `일반`, `기초`, `6%`, `9%`이며 기본 표시는 `일반`이다.
- 현재 선택값은 W1C 혜택 이력 API에 저장하도록 연결돼 있다.
- 상세 화면의 W1C·계획서 통보일·계약 생성은 `detail-batch` 요청 안에서 같은 SQLAlchemy 세션을 사용하고 마지막에 한 번만 커밋한다.
- 이력 무효화와 계약 전환은 일반 초안 저장에 섞지 않고 각각의 독립 원자 명령으로 유지한다.

### 3. 반드시 해결할 잔여 위험

현재 수급자 기본정보·보호자 저장과 본인부담금 W1C 이력 저장은 서로 다른 HTTP 요청이다. 기본정보 저장이 성공한 뒤 본인부담금 저장이 실패하면 기본정보만 남을 수 있다. 수급자 등록도 같은 위험이 있다.

다음 작업의 최우선 목표는 아래 작업을 각각 하나의 DB 트랜잭션으로 만드는 것이다.

- 기존 수급자 수정: 수급자 기본정보 + 보호자 1·2 + 납부자 지정 + 본인부담금 이력
- 신규 수급자 등록: 수급자 생성 + 보호자 1·2 생성 + 납부자 지정 + 최초 본인부담금 이력

어느 단계에서든 실패하면 그 요청에서 수행한 모든 쓰기를 롤백해야 한다. 프런트에서 여러 API를 순서대로 호출해 원자성을 흉내 내면 안 된다.

### 4. 백엔드 구현 지시

1. 수급자 기본 수정용 일괄 요청과 신규 등록용 일괄 요청의 명시적 Pydantic 계약을 만든다.
2. 요청에는 수급자 payload, 보호자 슬롯별 create/update payload, 납부자 선택, 선택적 W1C benefit mutation을 포함한다.
3. 수급자·보호자·W1C 서비스가 동일한 request-scoped SQLAlchemy `Session` 객체를 사용하는지 fail-closed로 확인한다.
4. 각 도메인 서비스의 중간 `commit()`은 일괄 처리 범위에서 금지하고 필요한 `flush()`만 수행한다.
5. 신규 등록은 수급자 ID를 flush로 확보한 뒤 보호자를 생성하고, 생성된 실제 guardian ID로 납부자를 지정한 다음 benefit period를 만든다.
6. 수정은 recipient, guardian, benefit period의 `expected_row_version`을 모두 검증한다.
7. 하나의 충돌이라도 발생하면 HTTP 409로 반환하고 전체 트랜잭션을 롤백한다.
8. 무결성 오류와 예상하지 못한 DB 오류도 롤백 후 기존 오류 envelope 형식으로 반환한다.
9. 성공 응답에는 생성·수정된 수급자, 보호자, 납부자, benefit 결과와 저장된 섹션 목록을 명시한다.
10. 권한 및 CSRF 조건은 기존 `RecipientManageAccountDependency`와 동일하게 유지한다.

### 5. 본인부담금 계약 지시

- 화면 `일반`은 `GENERAL`에 대응한다.
- 화면 `기초`는 `BASIC_LIVELIHOOD`에 대응한다.
- 화면 `6%`와 `9%`는 현재 스키마의 `REDUCTION_6`, `REDUCTION_9`, `MEDICAL_6`, `MEDICAL_9`와 의미가 겹친다.
- 기존 값이 `MEDICAL_6` 또는 `MEDICAL_9`인 경우 화면 표시만 6%·9%로 합치더라도, 사용자가 값을 변경하지 않은 저장에서 이를 `REDUCTION_6`·`REDUCTION_9`로 바꾸면 안 된다.
- 사용자가 6%·9%를 새로 선택했을 때 어떤 canonical code를 생성할지는 제품 계약으로 확정한다. 확정 전에는 임의 변환을 새 데이터에 적용하지 않는다.
- 적용 시작일은 필수이며 본인부담금 카드 내부에 유지한다.
- `일반`을 단순한 null 표시로만 취급할지 명시적 `GENERAL` 기간으로 저장할지 계약을 확정한다. 원자 등록 구현에서는 명시적 `GENERAL` 저장을 우선안으로 검토한다.
- 미래 시작일을 기존 기간의 단순 정정 replacement로 처리해 과거 이력을 지우지 않는다. 새 기간 생성·기존 기간 종료 규칙을 계약과 DB 제약에 맞게 구현한다.

### 6. 프런트엔드 구현 지시

1. 현재 `pendingBasicCopaySave`, `pendingCreateCopaySave` 및 저장 완료 후 후속 W1C 요청을 보내는 흐름을 제거한다.
2. 기본 수정의 상단 `저장`은 수정 일괄 API를 정확히 한 번만 호출한다.
3. 수급자 등록의 `저장`은 등록 일괄 API를 정확히 한 번만 호출한다.
4. 성공 전에는 편집 상태를 닫거나 URL 선택값을 바꾸지 않는다.
5. 409 충돌이나 유효성 오류가 발생하면 입력값을 그대로 유지하고 사용자에게 작은 경고 팝업을 표시한다.
6. `취소`는 네트워크 쓰기 없이 모든 수급자·보호자·본인부담금 초안을 원래 값으로 되돌린다.
7. 저장 중에는 수급자 전환, 기본/상세 전환, 중복 저장을 막는다.
8. 기본·수정·등록 화면은 같은 DOM 입력 요소, 카드 좌표, 라벨 좌표, 값 폰트와 줄 높이를 유지한다.
9. 기본 화면은 같은 요소를 `inert` 또는 읽기 전용으로만 잠그고 별도 `<strong>` 표시 분기를 다시 만들지 않는다.
10. 보호자 1·2도 조회·수정·등록에서 동일 입력 요소를 사용한다.
11. 월별 테마색은 수정·등록 상태에만 적용하되 요소 크기와 좌표를 바꾸지 않는다.

### 7. 트랜잭션 완료 조건

- 저장 또는 등록 한 번당 제품 쓰기 HTTP 요청은 정확히 1개다.
- 수급자 저장 후 별도의 guardian 또는 W1C 저장 요청이 발생하지 않는다.
- 수급자, 보호자 1, 보호자 2, 납부자, benefit 각 단계에 실패를 주입해도 DB before/after가 완전히 동일하다.
- 마지막 단계 benefit 저장 실패에서도 앞선 수급자·보호자 쓰기가 남지 않는다.
- stale recipient, stale guardian, stale benefit row version은 모두 409이며 쓰기 수는 0이다.
- 같은 요청을 중복 전송해도 중복 보호자 또는 중복 benefit 기간이 조용히 생성되지 않는다.
- 성공한 신규 등록은 선택한 납부자가 실제 생성된 guardian ID를 가리킨다.

### 8. 테스트 지시

- 백엔드 서비스 테스트: 각 하위 단계 실패 시 전체 rollback을 검증한다.
- PostgreSQL 통합 테스트: 실제 제약과 row lock 아래에서 before/after 테이블 스냅샷을 비교한다.
- API 테스트: 권한, CSRF, 422, 409, 500 오류 envelope와 쓰기 0건을 확인한다.
- 프런트 테스트: 저장 클릭당 일괄 API 1회, 기존 개별 API 0회를 검증한다.
- 프런트 테스트: 취소 시 요청 0회 및 모든 초안 복원을 검증한다.
- 프런트 테스트: 오류 후 입력값과 편집 모드가 유지되는지 확인한다.
- 브라우저 확인: 기본·수정·등록의 이름, 생년월일, 성별, 보호자 값의 좌표와 글꼴이 동일한지 확인한다.
- 브라우저 확인: 본인부담금 선택과 적용 시작일이 같은 카드 안에서 잘리지 않는지 확인한다.
- 브라우저 확인: 수정 중 기본정보 전환과 다른 수급자 선택이 차단되는지 확인한다.

### 9. 비범위

- W1D 계약 전환과 이력 무효화를 기본 일괄 저장에 포함하지 않는다.
- 수급자 대기 상태를 계약 만료나 이력으로 추론하지 않는다.
- 임의의 새 DB 컬럼으로 W1C 혜택 이력을 이중 저장하지 않는다.
- 화면 표시만 되는 선택값이나 localStorage 임시 저장으로 완료 처리하지 않는다.

### 10. 현재 검증 상태

- 이번 UI 및 일괄 저장 변경 이후 테스트, 빌드, 브라우저 동작 검증은 아직 실행하지 않았다.
- Vite 미리보기는 `127.0.0.1:5174`에서 유지 중인 상태를 전제로 한다.
- 다음 작업자는 검증을 실행하기 전에 현재 서버 프로세스와 실제 serving cwd가 `E:\sswcenter\w1f-candidate\frontend`인지 확인한다.

### 11. 완료 보고 형식

- 실제 repo root와 frontend 경로
- 변경 파일 목록
- 단일 트랜잭션 경계 설명
- 제거한 순차 요청 목록
- 실패 주입별 rollback 결과
- 프런트 요청 횟수 검증 결과
- 브라우저 좌표·표시 확인 결과
- 실행한 테스트 명령과 exit code
- 미실행 항목 및 잔여 위험
- Git 변경은 보고만 하고 별도 지시 없이 stage, commit, push하지 않는다.
